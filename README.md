# python_education

Как запушить код в отдельной ветке (например для домашки):
1. имя ветки формируем так: <номер занятия>_dz. например "2_dz"
2. создаем ветку: ```git checkout -b имя_ветки```
3. ```git add .```
4. ```git commit -m "какой-то текст"```
5. ```git push```
